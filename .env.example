# Environment Configuration Template
# Copy this to a .env file and customize for your setup

# Server Configuration
# If empty, server will auto-detect network IP
# Set to specific IP if auto-detection fails
SERVER_IP=

# ML API Configuration
# Should match SERVER_IP unless ML runs on different machine
ML_API_IP=
ML_API_PORT=5000

# Server Port
# Usually 3000, change if port is already in use
PORT=3000

# Examples:
# SERVER_IP=192.168.1.100
# ML_API_IP=192.168.1.101
# ML_API_PORT=5000
# PORT=3000
